<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Payment deep links. One filter covers every scheme: with
                 only android:scheme set and no host/path, each <data> is an
                 independent match, so the per-scheme filters Misty Breez
                 spells out separately collapse into this block.

                 lnurlp / lnurlw / keyauth are the LUD-17 schemes. lnurlc
                 (LNURL-channel) is deliberately absent even though Misty
                 claims it: Glow has no channel flow, so taking the link
                 would only dead-end it away from an app that can service it.
                 No android:autoVerify here either: it only means something
                 for http/https App Links.

                 bitcoin (BIP-21) goes further than Misty claims, because
                 Glow can actually settle one: the send sheet unwraps the
                 URI to its underlying method and runs the on-chain flow.

                 The SDK's parser takes these URIs whole, scheme prefix
                 included. `glow` is our own scheme and is stripped on the
                 web side before parsing.

                 Debug builds carry the .dev applicationId suffix, so a
                 device with both installed shows a chooser. That's the
                 intended behaviour, not a conflict. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lightning" />
                <data android:scheme="bitcoin" />
                <data android:scheme="lnurlp" />
                <data android:scheme="lnurlw" />
                <data android:scheme="keyauth" />
                <data android:scheme="glow" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Android 11+ package visibility: required for AppLauncher.openUrl to
         hand off cash.app universal links to Cash App. -->
    <queries>
        <package android:name="com.squareup.cash" />
    </queries>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- Camera is optional: the QR scanner falls back to image upload when
         camera is unavailable, so the app should install on devices without
         a camera (e.g., some tablets and automotive devices). -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
</manifest>
