# Glow App — Native Capacitor Wrapper

## Context

Wrap glow-web (React+Vite+WASM Bitcoin wallet) with Capacitor for native iOS/Android distribution. New repo `breez/glow-app` with glow-web as a git submodule.

**Priorities:**
1. Native shell with WASM working in WebView
2. Native passkey PRF flow (WebAuthn unavailable in WebViews — need native bridge)
3. Seed stored in iOS Keychain / Android Keystore (biometric-protected)
4. Later: push notifications for Lightning address payments

**Key resource**: Spark SDK PR #781 (`pr/passkey-core`) has native iOS/Android PRF provider code we'll reuse.

---

## Repo Structure

```
breez/glow-app/
  README.md                    # Setup, architecture, status
  PLAN.md                      # This plan (lives in repo)
  CLAUDE.md                    # Claude Code context
  DEVELOPMENT.md               # Full setup guide
  capacitor.config.ts          # Capacitor config (loggingBehavior pinned to 'none')
  package.json                 # Dependencies
  tsconfig.json
  glow-web/                    # git submodule -> breez/glow-web
  ios/                         # Xcode project (generated by Capacitor)
  android/                     # Android Studio project (generated by Capacitor)
  plugins/
    capacitor-passkey-prf/     # Native passkey PRF bridge
      package.json
      src/                     # TypeScript interface + web fallback
      ios/                     # Swift (ASAuthorization + PRF extension, from SDK PR #781)
      android/                 # Kotlin (CredentialManager + PRF extension, from SDK PR #781)
    capacitor-native-vault/    # Biometric-bound Keychain / Keystore seed storage
      package.json
      src/                     # TypeScript interface + typed error codes
      ios/                     # Swift (Keychain with SecAccessControl.biometryCurrentSet)
      android/                 # Kotlin (Keystore AES-GCM with setUserAuthenticationRequired + BiometricPrompt.CryptoObject)
```

### Build flow
```
cd glow-web && npm run build    # produces glow-web/dist/
npx cap sync                    # copies dist/ into ios/ and android/ projects
npx cap open ios                # open in Xcode, build & run
npx cap open android            # open in Android Studio, build & run
```

---

## Phase 1: Repo Setup & Capacitor Scaffold

### 1.1 Create repo
- `gh repo create breez/glow-app --private`
- Add glow-web as submodule: `git submodule add https://github.com/breez/glow-web.git glow-web`
- Create README.md, PLAN.md, CLAUDE.md

### 1.2 Initialize Capacitor project
- `npm init -y`
- Install: `@capacitor/core`, `@capacitor/cli`, `@capacitor/ios`, `@capacitor/android`
- `npx cap init --name Glow --id technology.breez.glow --webDir glow-web/dist`

### 1.3 Configure `capacitor.config.ts`
- `webDir: 'glow-web/dist'` (submodule build output)
- COOP/COEP headers for SharedArrayBuffer:
  ```ts
  server: {
    androidScheme: 'https',
    headers: {
      'Cross-Origin-Embedder-Policy': 'require-corp',
      'Cross-Origin-Opener-Policy': 'same-origin',
    },
  }
  ```

### 1.4 Add native platforms
- `npx cap add ios && npx cap add android`
- Commit `ios/` and `android/` directories

### 1.5 Build & verify
- Build glow-web: `cd glow-web && npm install && npm run build`
- Sync: `npx cap sync`
- Open iOS: `npx cap open ios` -- verify app loads, WASM inits, no crashes
- Passkey expected to fail gracefully at this point (`isPrfAvailable()` -> false)

### Files created
- `package.json`, `capacitor.config.ts`, `tsconfig.json`
- `README.md`, `PLAN.md`, `CLAUDE.md`, `.gitignore`, `.gitmodules`
- `ios/`, `android/` (generated)

---

## Phase 2: Passkey PRF Capacitor Plugin

Native bridge for passkey PRF. No existing Capacitor plugin supports the PRF extension. Native code comes from SDK PR #781.

### 2.1 Plugin structure
```
plugins/capacitor-passkey-prf/
  package.json                  # capacitor plugin manifest
  src/
    definitions.ts              # TS interface: isPrfAvailable, createPasskey, derivePrfSeed
    index.ts                    # registerPlugin
    web.ts                      # Web fallback using navigator.credentials (WebAuthn)
  ios/Sources/PasskeyPrfPlugin/
    PasskeyPrfPlugin.swift      # Capacitor bridge (@objc methods)
    PlatformPasskeyPrfProvider.swift   # From SDK PR #781 (ASAuthorization)
    PasskeyPRFHelper.h/.m       # ObjC helper for PRF types
  android/src/main/kotlin/.../
    PasskeyPrfPlugin.kt         # Capacitor bridge (@PluginMethod)
    CredentialManagerPrfCore.kt  # From SDK PR #781 (CredentialManager)
  android/build.gradle          # androidx.credentials deps
```

### 2.2 Plugin TypeScript interface
```ts
isPrfAvailable(): Promise<{ available: boolean }>
createPasskey(options: { rpId, rpName, userName?, userDisplayName? }): Promise<void>
derivePrfSeed(options: { rpId, salt }): Promise<{ seed: string }> // base64-encoded 32 bytes
```

### 2.3 Integration with glow-web

Runtime detection in glow-web (PR to breez/glow-web):
- `src/services/capacitorPasskeyPrfProvider.ts` -- implements `PasskeyPrfProvider` via Capacitor plugin bridge
- Factory in `passkeyPrfProvider.ts`: `Capacitor.isNativePlatform() ? CapacitorProvider : BrowserProvider`
- `@capacitor/core` as optional peer dependency (no-op on web)

### 2.4 Platform config required
- **iOS**: Associated Domains entitlement `webcredentials:keys.breez.technology`, deployment target iOS 18+
- **Android**: `assetlinks.json` registered on `keys.breez.technology`, minSdk 28, credentials deps
- **Both**: Contact Breez team to register `technology.breez.glow` on `keys.breez.technology`

### Verify
- iOS simulator: passkey creation triggers ASAuthorization sheet, PRF returns 32-byte seed, wallet connects
- Android device (emulators often lack credential providers): CredentialManager dialog, same flow
- Web browser: regression -- existing passkey flow unchanged

---

## Phase 3: Native Secure Seed Storage

Cache the wallet seed in iOS Keychain / Android Keystore with biometric binding enforced at the OS layer. Repeat launches: one biometric prompt instead of the full passkey ceremony.

Shipped in two PRs:
- **#2 (initial)** — built on top of the `@aparajita/capacitor-secure-storage` and `@aparajita/capacitor-biometric-auth` packages. Biometric was an external gate in front of the vault, not bound to the crypto operation.
- **#3 (follow-ups)** — replaced the aparajita dependencies with the in-house `capacitor-native-vault` plugin, added biometric binding at the cryptographic layer, added a dedicated unlock screen, and corrected a misread Capacitor logging config that had been leaving plaintext wallet material in system logs.

### 3.1 Secure storage abstraction

`glow-web/src/services/secureStorage.ts` — the single entry point. Exposes `isSupported` / `hasStoredSeed` / `storeSeed` / `retrieveSeed` / `clearSeed` with a typed `SecureStorageError` carrying one of `USER_CANCELLED` / `BIOMETRIC_LOCKOUT` / `BIOMETRIC_NOT_ENROLLED` / `BIOMETRIC_UNAVAILABLE` / `KEY_INVALIDATED` / `NO_STORED_SEED` / `NOT_SUPPORTED` / `UNKNOWN`. Runtime factory returns a `NativeSecureStorage` on Capacitor hosts and a `NoopSecureStorage` everywhere else. glow-web never touches plugin internals — everything goes through this one file.

### 3.2 In-house `capacitor-native-vault` plugin

`plugins/capacitor-native-vault/` owns the native crypto on both platforms. Trait-based providers (Swift protocol / Kotlin interface) split biometric auth and seed vault into separate files so each concern is testable in isolation.

- **iOS**: Keychain items protected by `SecAccessControl` with `[.biometryCurrentSet]` + `kSecAttrAccessibleWhenUnlockedThisDeviceOnly`. The biometric prompt is triggered inline by `SecItemCopyMatching` — no separate `LAContext.evaluatePolicy` pre-step. Adding a new Face ID / Touch ID enrollment voids the item automatically (Apple's recommended failsafe pattern). `hasStoredSeed` uses `kSecUseAuthenticationUIFail` for a presence check that doesn't trigger a prompt. `storeSeed` / `retrieveSeed` run on `Task.detached` so the blocking Keychain call doesn't freeze the WebView thread.
- **Android**: Keystore AES-GCM key generated with `setUserAuthenticationRequired(true)`, `setUserAuthenticationParameters(0, AUTH_BIOMETRIC_STRONG)` on API 30+, and `setInvalidatedByBiometricEnrollment(true)`. Per-operation auth means the cipher has to be wrapped in a `BiometricPrompt.CryptoObject` before any `doFinal()` call. The plugin orchestrates a three-step `prepare → authenticateWithCrypto → finish` flow per store/retrieve. `BiometricPromptAuth` hops to the main thread via `activity.runOnUiThread { }` because Capacitor 8 dispatches plugin methods on a worker thread by default, and `BiometricPrompt` uses FragmentManager transactions that require the main thread.

### 3.3 Integration with glow-web's `useBreezSdk.ts`

Three touch points:
- **Startup**: `checkForExistingWallet` tries `secureStorage.retrieveSeed` before falling through to legacy localStorage / passkey paths.
- **Post-connect**: `storeSeed` writes the seed under biometric binding when the seed was sourced from onboarding. Skipped entirely when the seed was just retrieved from secure storage (avoids a redundant Keystore round-trip on every relaunch).
- **Logout**: `clearSeed` alongside the existing localStorage cleanup. On Android this also deletes the Keystore key so subsequent launches start with a fresh one.

### 3.4 Dedicated unlock screen

When `retrieveSeed` encounters `USER_CANCELLED` or `BIOMETRIC_LOCKOUT`, `startupState` transitions to a dedicated `'native-locked'` route instead of falling through to the welcome screen — where the user could accidentally create a new passkey label and orphan the stored wallet. The unlock page has a "Unlock with Face ID / Fingerprint" CTA (label adapts via `NativeVault.checkBiometry`) and a "Use a different wallet" escape that wipes the vault and routes back to onboarding.

### 3.5 Capacitor `loggingBehavior` security fix

`capacitor.config.ts` pins `loggingBehavior: 'none'`. Capacitor's naming for this flag is **inverted** from what it sounds like — `'production'` means *always* log, and `'none'` is the value that actually suppresses bridge traces. The original #2 pin set `'production'` based on a misread and therefore did nothing; device verification on #3 caught it by grepping logcat for the mnemonic during a fresh onboarding run. Tradeoff: `'none'` also gates WebView `console.*` bridging, so structured-logger breadcrumbs from glow-web no longer appear in logcat — use the in-app log viewer (Settings → Share Logs) instead.

### Flow
```
App launch (native) → check native secure vault
  → stored seed found → OS-enforced biometric prompt → SDK.connect
  → biometric cancelled → dedicated unlock screen (retry or abandon)
  → no stored seed → fall through to passkey / mnemonic onboarding
```

### Verify
- **First launch**: passkey ceremony completes, "Enabling biometric unlock…" spinner appears, second biometric prompt fires for the `SecAccessControl` / `CryptoObject` store, wallet home loads
- **Second launch**: single inline biometric prompt → wallet home
- **Biometric cancel**: lands on the dedicated unlock screen (not welcome); retry works; "Use a different wallet" wipes and re-onboards
- **Logout**: `clearSeed` wipes the vault; next launch shows onboarding with no biometric prompt
- **Web**: existing localStorage / passkey flow unchanged
- **Logcat grep**: zero hits for `mnemonic`, `"seed"`, `methodData`, or `derivePrfSeed` after the `loggingBehavior: 'none'` fix

---

## Phase 4: Polish & Distribution

- App icons (1024x1024 source), splash screen (`@capacitor/splash-screen`, bg #0a0a0f)
- iOS: Info.plist (Face ID description, camera for QR), entitlements
- Android: manifest permissions, gradle config
- CI: GitHub Actions for building iOS/Android
- TestFlight / Play Store internal testing

---

## Implementation Status

| Phase | Status | Notes |
|-------|--------|-------|
| 1. Repo + Scaffold | ✅ Complete | |
| 2. Passkey Plugin | ✅ Complete | Merged in #1. E2E verified on iOS + Android. Depends on Spark SDK `pr/passkey-core`. |
| 3. Native Secure Vault | ✅ Complete | Merged in #2 (initial aparajita-backed version) and #3 (in-house `capacitor-native-vault` plugin, OS-enforced biometric binding, dedicated unlock screen, Capacitor `loggingBehavior` security fix). Verified on Honor EML_L09 + iPhone 14 (iOS 26.3.1) + web. |
| 4. Polish & Distribution | 🔜 Not Started | App icons, splash screen, CI, TestFlight / Play Store internal testing. |

## Post-Phase-3 Follow-ups

Captured for context — tracked and prioritized in the Glow App roadmap.

### Still outstanding

- **Opaque-handle pattern across the Capacitor bridge** — the proper fix for the `loggingBehavior: 'none'` pin. Right now the seed still crosses the JS↔native bridge as plaintext at SDK connect time and at vault store time; the Capacitor logging config suppresses the bridge traces, but that pin is the only thing keeping plaintext out of system logs. The correct fix is to keep PRF entropy on the native side of `capacitor-passkey-prf` and expose only an opaque handle to JS, then have `capacitor-native-vault` accept that handle directly so the seed never round-trips through the bridge in plaintext. Cross-plugin change involving both in-house plugins plus glow-web's `passkeyService` and `useBreezSdk.connectWallet`. Bigger design pass before implementation — open questions around handle scoping, cleanup policy, and whether SDK connect can accept a handle directly.

- **Publish the in-house Capacitor plugins as standalone packages** — both `capacitor-passkey-prf` and `capacitor-native-vault` currently live as `file:plugins/...` dependencies. Publishing would let other Breez SDK Spark users adopt them without vendoring, but requires a cleaner API surface (no glow-app-specific assumptions), npm release tooling, and ongoing maintenance commitment.

### Optional UX improvements

- **Onboarding double-biometric UX** — first-time onboarding shows 3-4 biometric prompts (passkey ceremony + secure-vault store). The "Enabling biometric unlock…" label helps contextualize the last one, but doesn't reduce the count. Two approaches if user feedback shows it's still confusing: lazy-binding migration (first store plain, promote on second launch) or an explicit opt-in "Enable biometric quick-unlock" setting.
