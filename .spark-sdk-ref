# Pinned spark-sdk commit that glow-app's build pipeline expects.
#
# The branch reference below is documentation only — CI checks out
# the SHA directly. When you need to move glow-app onto a newer
# spark-sdk commit, update the SHA on the last non-comment line
# and commit the change. scripts/resolve-spark-sdk.sh reads only
# the first non-comment, non-blank line as the SHA.
#
# Branch: proto/passkey-web-ux (breez/spark-sdk). Exploratory:
#   wasm-side signalRename / signalCurrentUserDetails surface
#   with defaultTimeoutMs ctor and explicit attestation: 'none',
#   feeding the matching glow-web passkey provider hardening.
#   Promote / merge alongside the corresponding glow-app + glow-web
#   branches once UX validation is done.
# Last verified: 2026-05-07.
68d35180cdac201ed9e723efe387a88de5ec9125
