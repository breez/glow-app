# Pinned spark-sdk commit that glow-app's build pipeline expects.
#
# The branch reference below is documentation only — CI checks out
# the SHA directly. When you need to move glow-app onto a newer
# spark-sdk commit, update the SHA on the last non-comment line
# and commit the change. scripts/resolve-spark-sdk.sh reads only
# the first non-comment, non-blank line as the SHA.
#
# Branch: proto/passkey-aaguid (breez/spark-sdk) — exploratory:
#   extends bulk-PRF path with AAGUID-aware provider resolution.
#   Promote / merge alongside the matching glow-app + glow-web
#   proto/passkey-aaguid branches once UX validation is done.
# Last verified: 2026-05-06 — refreshed after the previous SHA
# (71c9542c) was rebased away on origin and CI's setup-glow-app
# action started failing with "not our ref" / "unable to read tree".
a2dd78055a262f130f97a0da851252483729e387
