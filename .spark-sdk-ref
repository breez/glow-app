# Pinned spark-sdk commit that glow-app's build pipeline expects.
#
# The branch reference below is documentation only. CI checks out
# the SHA directly. When you need to move glow-app onto a newer
# spark-sdk commit, update the SHA on the last non-comment line
# and commit the change. scripts/resolve-spark-sdk.sh reads only
# the first non-comment, non-blank line as the SHA.
#
# Branch: 0.15.0-passkey-api-reduction (breez/spark-sdk). The
#   passkey/api-reduction work rebased onto the 0.15.0 release
#   tag, mirroring the 0.X.0-passkey-<variant> pattern used by
#   prior backport branches (0.14.0-passkey-web-ux,
#   0.15.0-passkey-web-ux). Same passkey surface as
#   passkey/api-reduction (checkAvailability + labels() /
#   credentials() sub-objects, connect_with_passkey unified
#   onboarding, per-call DeriveSeedsRequest, etc.) but with the
#   0.15.0 SDK base (token configs, shared http client, session
#   manager removed from Flutter binding, etc.).
# Last verified: 2026-05-23.
8f158631247460dd45ac63df19258b5652939e32
